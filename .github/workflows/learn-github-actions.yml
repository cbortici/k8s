name: learn-github-actions
run-name: ${{ github.actor }} is learning GitHub Actions
on: 
  workflow_dispatch:
    inputs:
      access_id:
        description: 'access id token'     
        required: true
        default: 'input access id token'
jobs:
  authentication:
    runs-on: ubuntu-latest
    outputs:
      HELLO: "${{ steps.auth.outputs.HELLO }}"
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
      - name: Auth
        id: auth
        run: |
            echo "HELLO=${{ inputs.access_id }}" >> "$GITHUB_OUTPUT"
      - name: one-2
        run: |
          echo "${{steps.auth.outputs.HELLO}}"
      # - name: Terraform Init
      #   id: init
      #   run: terraform init

      # - name: Terraform Validate
      #   id: validate
      #   run: terraform validate -no-color

      # - name: Terraform Plan
      #   id: plan
      #   run: |
      #       export AWS_ACCESS_KEY_ID=${{ steps.auth.outputs.accessID }}
      #       export AWS_SECRET_ACCESS_KEY=${{ steps.auth.outputs.secretID }}
      #       export AWS_REGION="eu-north-1"
      #       terraform plan
      #   continue-on-error: true
  terraform:
    needs:
      - authentication
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
      - name: terraform init
        id: init
        run: terraform init
      - name: Terraform Plan
        id: plan
        run: |
            echo "${{ needs.authentication.outputs.HELLO }}"
        continue-on-error: true

